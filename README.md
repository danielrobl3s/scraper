# scraper
This is a scrapers testing repository to www.larepublica.co

//First, you will need to create a new python environment:
#python -m venv venv

//Enter to it:
#source env/scripts/activate (on git bash)

#Install dependencies:
//pip install -r requirements.txt

#run scraper.py file:
//python scraper.py or its variants: py scraper.py, python3 scraper.py
